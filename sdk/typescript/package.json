{
  "name": "@hudson-infinity/sandbox-client",
  "version": "0.0.0",
  "private": true,
  "description": "Project-scoped HTTPS client for Hudson Sandbox",
  "license": "Apache-2.0",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=24.9.0" },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "npm run build && node --test tests/*.test.mjs" },
  "devDependencies": { "typescript": "7.0.2", "@types/node": "24.13.6" }
}
